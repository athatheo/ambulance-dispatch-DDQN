"""
distance(ambulance_loc, accident_loc)

input: zipcodes ambulances, accident
else: total_distance() -> when is ambulance available
      state

"""


def state_available(self,region, availability)
base_list= self.bases(region)
available_bases=[]
#int counter= total_distance            #count down counter
for i, base in enumerate(base_list):
    #available_bases.append(int base))       #list of bases per region
    if base_list -availability[i]>0
        available_bases.append(int(base))
    
    #availability [base1_amount, base2_amount... ]
    availability[i]=availability[i]-1


# read from sample-Accident where/when accident happend
# start counter       #count up

#counter needed to  +i -> when back to base     

total_time=

